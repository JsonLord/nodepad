#!/usr/bin/env node
const {LAYA_BASE_URL:base,LAYA_API_KEY:key,LAYA_MODEL:model}=process.env
if(!base||!model){console.log(JSON.stringify({event:"laya_probe_skipped",reason:"LAYA_BASE_URL and LAYA_MODEL are required"}));process.exit(0)}
const endpoint=(path)=>`${base.replace(/\/$/,"")}/${path}`,headers={"content-type":"application/json",...(key&&{authorization:`Bearer ${key}`})}
const fail=(reason)=>{console.error(JSON.stringify({event:"laya_probe_failed",reason}));process.exitCode=1}
try{const models=await fetch(endpoint("models"),{headers});const modelBody=models.ok?await models.json():null;const found=Array.isArray(modelBody?.data)?modelBody.data.some(x=>x.id===model):models.ok;if(!models.ok)throw new Error(`models_http_${models.status}`);if(!found)throw new Error("model_not_found");const payload={model,temperature:0,response_format:{type:"json_object"},messages:[{role:"system",content:"Return JSON with selectedAgentId and calibrated probabilities for every candidate."},{role:"user",content:JSON.stringify({routingContext:{workType:"probe"},candidates:{probe_a:{locality:"remote",capabilities:["analysis"]},probe_b:{locality:"remote",capabilities:["analysis"]}}})}]};const response=await fetch(endpoint("chat/completions"),{method:"POST",headers,body:JSON.stringify(payload)});if(!response.ok)throw new Error(`decision_http_${response.status}`);const raw=await response.json(),content=raw?.decision??JSON.parse(raw?.choices?.[0]?.message?.content??"null"),probabilities=content?.probabilities;if(!content?.selectedAgentId||!probabilities||!Object.hasOwn(probabilities,"probe_a")||!Object.hasOwn(probabilities,"probe_b"))throw new Error("structured_probabilities_unavailable");const sum=Object.values(probabilities).reduce((a,b)=>a+Number(b),0);if(!Object.values(probabilities).every(x=>Number.isFinite(x)&&x>=0&&x<=1)||Math.abs(sum-1)>.01||!Object.hasOwn(probabilities,content.selectedAgentId))throw new Error("invalid_probability_distribution");console.log(JSON.stringify({event:"laya_probe_ok",endpointReachable:true,modelFound:true,responseStructureRecognized:true,probabilitiesAvailable:true,selectedIsEligible:true}))}catch(e){fail(e instanceof Error?e.message:"unknown")}
